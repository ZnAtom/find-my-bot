services:
  db:
    build: .
    container_name: findmybot-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: password
      POSTGRES_DB: lostfound
      HF_LOCAL_ONLY: "1"
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U appuser -d lostfound" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      args:
        INSTALL_EMBEDDING_DEPS: ${INSTALL_EMBEDDING_DEPS:-0}
    container_name: findmybot-backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://appuser:password@db:5432/lostfound
      - DB_USER=appuser
      - DB_PASSWORD=password
      - DB_NAME=lostfound
      - DB_HOST=db
      - DB_PORT=5432
      - HF_ENDPOINT=https://hf-mirror.com
      - HF_LOCAL_ONLY=1
      - EMBEDDING_ENABLED=${EMBEDDING_ENABLED:-0}
      - EMBEDDING_MODEL_PATH=/app/model/models--Qwen--Qwen3-VL-Embedding-2B
      - SUPPORT_DOCS_DIR=/app/doc/support
      - QQ_BOT_SERVICE_TOKEN=${QQ_BOT_SERVICE_TOKEN:-}
      - QQ_SUPPORT_RATE_LIMIT_COUNT=${QQ_SUPPORT_RATE_LIMIT_COUNT:-60}
      - QQ_IMAGE_SEARCH_RATE_LIMIT_COUNT=${QQ_IMAGE_SEARCH_RATE_LIMIT_COUNT:-20}
    volumes:
      - ./backend/uploads:/app/uploads
      - ./doc:/app/doc:ro
      - ./model:/app/model:ro
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-network

  frontend:
    build: ./frontend
    container_name: findmybot-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:5173:5173"
    environment:
      - VITE_PROXY_TARGET=http://backend:8000
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/vite.config.js:/app/vite.config.js
      - ./frontend/.env.development:/app/.env.development
      - ./frontend/.env.production:/app/.env.production
      - ./frontend/index.html:/app/index.html
    networks:
      - app-network
  # QQ 机器人服务（需要时取消注释，镜像可能需先手动拉取）
  # napcat:
  #   image: napneko/napcatqq:latest
  #   container_name: napcat
  #   restart: unless-stopped
  #   volumes:
  #     - ./qqbot/napcat/config:/app/config
  #     - ./qqbot/napcat/data:/app/data
  #   ports:
  #     - "3001:3001"
  #     - "8080:8080"
  #   environment:
  #     - TZ=Asia/Shanghai
  #   networks:
  #     - app-network
  #
  # astrbot:
  #   image: soulter/astrbot:latest
  #   container_name: astrbot
  #   restart: unless-stopped
  #   volumes:
  #     - ./qqbot/astrbot/config:/AstrBot/data
  #   ports:
  #     - "6185:6185"
  #     - "6199:6199"
  #   environment:
  #     - TZ=Asia/Shanghai
  #   depends_on:
  #     - napcat
  #   networks:
  #     - app-network

volumes:
  pgdata:

networks:
  app-network:
    driver: bridge

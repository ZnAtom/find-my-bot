apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - db.yaml
  - frontend.yaml

# These image tags will be replaced dynamically by the CI/CD pipeline
images:
  - name: ghcr.io/znatom/findmybot-db
    newTag: latest
  - name: ghcr.io/znatom/findmybot-frontend
    newTag: latest

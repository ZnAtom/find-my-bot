你的框架非常清晰，抓住了核心痛点——**简化输入**（定位/图片代替文字）、**智能匹配**（自动挂失与主动询问）、**多渠道交互**（网页+QQ）。下面我来判断合理性、补充细节，并规范化成完整的技术方案。

---

# 校园失物招领 AI Agent - 用户交互与数据处理流程（详细设计）

---

## 一、整体架构与数据流总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                         数据入口层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │  网页端      │  │  QQ机器人    │  │  定时任务    │            │
│  │ (手动发布)   │  │ (群/私聊)   │  │ (自动采集)   │            │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘            │
│         │                  │                  │                    │
│         └──────────────────┼──────────────────┘                    │
│                            ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                    统一数据处理层                           │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │ 图片OCR  │  │ 文本清洗 │  │ 位置解析 │  │ 时间解析 │  │  │
│  │  │ (Qwen-VL)│  │ (NLP)   │  │ (Geocoder)│  │ (正则)  │  │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │  │
│  └──────────────────────────┬──────────────────────────────────┘  │
│                             ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                    数据存储层                               │  │
│  │  ┌────────────────┐  ┌────────────────┐                    │  │
│  │  │  PostgreSQL    │  │  Pgvector      │                    │  │
│  │  │ (结构化数据)   │  │ (文本向量)     │                    │  │
│  │  └────────────────┘  └────────────────┘                    │  │
│  └──────────────────────────┬──────────────────────────────────┘  │
│                             ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                    智能决策层                               │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │ 匹配引擎 │  │ 挂失管理 │  │ 通知服务 │  │ 权限控制 │  │  │
│  │  │ (相似度) │  │ (状态机) │  │ (QQ私聊) │  │ (RBAC)  │  │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │  │
│  └─────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、用户交互流程（分入口详述）

### A. 网页端交互流程

#### 1. 发布失物/寻物信息

**用户操作路径**：
1. 用户访问网页（Q: 如何登录？）
2. 点击「发布」按钮
3. 选择类型：「我捡到了」/「我丢了」
4. **简化交互**（你的核心需求）：
   - **定位**：点击「获取当前位置」按钮，浏览器API获取GPS/基站定位，自动填入地点（如"图书馆3楼"）
   - **图片上传**：上传1-3张照片，后端自动OCR提取物品描述、时间、地点
   - **时间**：默认当前时间，支持手动修改
   - **物品名称**：可选填（OCR识别不准确时可手动补）
5. 点击提交，数据进入处理流程

**补充细节**：
- 位置解析：需要把定位解析成教学楼、图书馆等...
- 图片OCR：调用**Qwen-VL API**，Prompt提取结构化JSON
- **兜底机制**：如果OCR识别置信度<70%，标记为`待人工审核`，并提醒用户手动补充关键字段

#### 2. 查询物品

**用户操作路径**：
1. 用户输入自然语言查询（如"我上周在图书馆丢的蓝色杯子"）
2. 把自然语言结构化，询问缺失信息，比如杯子有什么特征码？有补充的照片吗？
3. 获取信息后，向用户确认信息是否准确？
4. 系统同时执行：
   - **向量搜索**：在pgvector中检索语义最相似的失物/寻物记录
   - **结构化过滤**：提取时间（"上周"→时间范围）、地点（"图书馆"）进行预过滤
5. 返回匹配列表（按相似度降序）
6. 若无匹配（相似度<阈值），自动创建「挂失单」

**补充细节**：
- 结构化提取：把自然语言输出成固定格式的josn
- 阈值设定：
  - **匹配成功**：相似度 ≥ 0.75 → 展示匹配项
  - **疑似匹配**：0.5 ≤ 相似度 < 0.75 → 展示并标注"疑似匹配，请自行核实"
  - **无匹配**：相似度 < 0.5 → 自动创建挂失

#### 3. 主动挂失与自动关注

**用户操作路径**：
1. 用户点击「挂失」按钮（或查询无结果时系统引导）
2. 填写物品信息（同样支持定位+图片简化输入）
3. 系统创建挂失记录，状态设为`active`
4. **后台定时任务**（每天或者每半天）：
   - 扫描所有`active`挂失记录
   - 与新入库的失物进行向量匹配
   - 若相似度 ≥ 0.8，触发通知流程
5. 通知方式：
   - 网页端消息中心弹窗
   - QQ私聊（若用户绑定了QQ号）
   - 邮件（可选）

**补充细节**：
- 挂失有效期：默认**7天**，超过自动关闭（可手动续期）
- 状态机设计：
  ```
  active → (匹配成功) → matched → (用户确认) → resolved
         → (超时) → expired
         → (用户取消) → cancelled
  ```

---

### B. QQ机器人交互流程

#### 1. 群聊交互（仅当被@时响应）

**触发条件**：群内有人 `@机器人` + 消息内容

**指令设计**（兼顾自然语言与快捷指令）：

| 用户输入示例                 | 系统理解 | 处理动作                                   |
| ---------------------------- | -------- | ------------------------------------------ |
| `@小助手 我丢了蓝色杯子`     | 挂失请求 | 创建挂失记录，返回"已记录，有消息会通知您" |
| `@小助手 我捡到一张校园卡`   | 失物上报 | 引导用户上传图片，创建拾物记录             |
| `@小助手 查一下我的杯子`     | 查询请求 | 向量检索并返回匹配结果                     |
| `@小助手 /help`              | 帮助菜单 | 展示所有指令                               |
| `@小助手 /banli`             | 补办流程 | 返回预设的补办指南                         |
| `@小助手 图书馆有我的杯子吗` | 条件查询 | 提取"图书馆"作为地点过滤 + 向量检索        |

**交互逻辑**：
1. 消息 → NoneBot2接收
2. 判断是否@机器人（是 → 处理；否 → 忽略）
3. 消息预处理：
   - 去除@提及部分
   - 调用模型进行意图识别（`intent: query/lost/found/help`）
   - 提取结构化实体（物品、时间、地点）
4. 根据意图调用对应功能模块
5. 生成回复并发送

**补充细节**：
- **防刷机制**：同一用户每分钟最多调用5次
- **上下文记忆**：对于"查询"等指令，缓存前后的相关消息
- **非法内容过滤**：接入**百度AI内容审核**或**阿里云绿网**，过滤敏感词

#### 2. 私聊交互

**触发条件**：用户直接给机器人发送消息

**交互逻辑**：
- 与群聊@相同，但**无需@前缀**
- 支持**连续对话**（维护session上下文）
- 用户**绑定qq**后，可以接收主动推送的匹配通知
- 自动推送消息需要有所限制，避免过渡打扰

**绑定流程**：（以下内容是ai生成的，但是我不确定到时候我们需要什么信息，来验证身份）
1. 用户发送 `@小助手 /bind 学号 姓名`
2. 机器人向网页端API验证身份（学号+姓名）
3. 验证通过，记录 `qq号 ↔ 学号` 映射关系
4. 后续匹配成功，可主动私聊推送通知

**补充细节**：
- **隐私保护**：绑定成功后，发送敏感信息（如联系方式）时，先询问用户是否同意披露

## 三、数据处理流程（核心）

### 1. 数据入库流程（以网页发布为例）

```
用户提交表单
    │
    ▼
┌──────────────────────────────────────┐
│ 1. 数据预处理                         │
│  - 图片 → Qwen-VL（举例） OCR → JSON  │
│  - 位置 → 逆地理编码 → 校区楼栋        │
│  - 时间 → 标准化为 timestamp          │
│  - 文本 → 去除emoji/特殊符号          │
└──────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────┐
│ 2. 结构化存储（PostgreSQL）           │
│  - 写入 items 表                      │
│  - 记录状态：pending_ocr/active       │
└──────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────┐
│ 3. 向量化（异步）                     │
│  - 将描述文本 (item_name + desc)      │
│  - 通过 BGE（or qw3） 模型 → 向量      │
│  - 存入 items_vectors 表 (pgvector)   │
└──────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────┐
│ 4. 触发匹配任务（异步）               │
│  - 若为失物 → 匹配挂失清单            │
│  - 若为寻物 → 匹配拾物清单            │
│  - 相似度 ≥ 0.8 → 触发通知           │
└──────────────────────────────────────┘
```

### 2. 向量化细节（重点）

**文本组合策略**（决定向量质量）：
```python
# 不要只用物品名，要组合多字段
text_for_embedding = f"""
物品名称：{item_name}
描述：{description}
地点：{location}
时间：{time}
颜色：{color}
信息来源：{qq/web}
"""
```

**模型选择**：推荐 `BAAI/bge-large-zh-v1.5`（768维）
- 在CPU上可运行（需2-4GB内存）
- 中文语义理解效果优异

**索引优化**：pgvector支持**IVFFlat**或**HNSW**索引，10万条数据下查询速度<100ms


# 任务分解

---

## 一、项目当前状态评估

| 模块          | 状态             | 说明                                                    |
| ------------- | ---------------- | ------------------------------------------------------- |
| QQ机器人      | ✅ 已完成基础功能 | 群内@可调用AI并回复                                     |
| Embedding模型 | ✅ 已部署         | 本地部署，可用                                          |
| 数据库        | ⚠️ 部分完成       | 有相关代码，但需完善表结构和索引                        |
| 前端网站      | ⚠️ 基础可用       | 页面能打开，但有bug：缺少验证、发布页面卡住、样式需优化 |
| 后端API       | ❌ 未完善         | 需要完整的CRUD和业务逻辑                                |
| OCR图片处理   | ❌ 未实现         | 需要集成Qwen-VL或其他OCR服务                            |
| 智能匹配引擎  | ❌ 未实现         | 向量检索+结构化过滤的完整匹配逻辑                       |
| 通知服务      | ❌ 未实现         | 匹配成功后的通知机制                                    |

---

## 二、任务分解总览

### Phase 1: 核心功能开发（优先完成网站）

#### 模块A：后端API开发（FastAPI）

**负责人：A同学**

| 序号 | 任务                                                  | 优先级 | 预估工时 | 依赖   |
| ---- | ----------------------------------------------------- | ------ | -------- | ------ |
| A1   | 项目初始化与基础配置                                  | P0     | 2h       | 无     |
| A2   | 数据库连接与ORM配置（SQLAlchemy）                     | P0     | 3h       | A1     |
| A3   | 数据表设计与迁移脚本（items、users、notifications等） | P0     | 4h       | A2     |
| A4   | 物品CRUD API（发布、查询、修改、删除）                | P0     | 6h       | A3     |
| A5   | 向量检索API（pgvector查询、相似度计算）               | P0     | 4h       | A3     |
| A6   | 智能匹配引擎（向量化+匹配逻辑）                       | P1     | 8h       | A4, A5 |
| A7   | 挂失管理API（状态机、自动匹配任务）                   | P1     | 6h       | A4, A6 |
| A8   | 用户认证与权限API（登录、注册、绑定QQ）               | P2     | 6h       | A3     |
| A9   | 通知服务API（消息中心、推送）                         | P2     | 4h       | A7     |

**技术要点：**
- 使用 SQLAlchemy + Alembic 进行数据库管理
- pgvector 索引选择 HNSW（10万条数据下查询速度<100ms）
- Embedding 模型推荐 `BAAI/bge-large-zh-v1.5`（768维）
- 文本组合策略：`物品名称 + 描述 + 地点 + 时间 + 颜色 + 信息来源`

---

#### 模块B：前端开发（Vue）

**负责人：B同学**

| 序号 | 任务                                     | 优先级 | 预估工时 | 依赖       |
| ---- | ---------------------------------------- | ------ | -------- | ---------- |
| B1   | 项目初始化与路由配置                     | P0     | 2h       | 无         |
| B2   | 首页设计（失物列表、搜索框）             | P0     | 4h       | B1         |
| B3   | 发布页面（表单验证、图片上传）           | P0     | 6h       | B1, A4     |
| B4   | 搜索页面（自然语言查询、结果展示）       | P0     | 6h       | B1, A5     |
| B5   | 个人中心（我的发布、我的挂失、消息中心） | P1     | 6h       | B1, A7, A9 |
| B6   | 样式优化（美观度提升、响应式适配）       | P2     | 4h       | B2-B5      |
| B7   | 用户认证页面（登录、注册、QQ绑定）       | P2     | 4h       | B1, A8     |
| B8   | 地图定位功能（获取当前位置）             | P2     | 3h       | B3         |

**技术要点：**
- 使用 Element Plus 组件库
- 表单验证使用 VeeValidate
- 图片上传需支持预览和压缩
- 搜索结果需按相似度排序并标注匹配程度

---

#### 模块C：AI与数据处理

**负责人：C同学**

| 序号 | 任务                               | 优先级 | 预估工时 | 依赖 |
| ---- | ---------------------------------- | ------ | -------- | ---- |
| C1   | Embedding服务封装（调用本地模型）  | P0     | 3h       | 无   |
| C2   | OCR服务集成（图片→结构化JSON）     | P0     | 6h       | 无   |
| C3   | 自然语言解析（意图识别+实体提取）  | P1     | 6h       | C1   |
| C4   | 定时匹配任务（Celery/Apscheduler） | P1     | 4h       | A7   |
| C5   | 重排序Rerank模型（可选优化）       | P3     | 4h       | A5   |
| C6   | 敏感内容过滤（可选）               | P3     | 2h       | A4   |

**技术要点：**
- OCR使用 Qwen-VL API 或本地部署的多模态模型
- 自然语言解析输出固定格式JSON（物品、时间、地点、颜色等）
- 定时任务建议使用 Apscheduler（轻量）或 Celery（分布式）

---

### Phase 2: QQ机器人完善

**负责人：A/C同学协作**

| 序号 | 任务                        | 优先级 | 预估工时 | 依赖   |
| ---- | --------------------------- | ------ | -------- | ------ |
| Q1   | 意图识别与实体提取集成      | P1     | 4h       | C3     |
| Q2   | 群聊指令处理（@机器人响应） | P1     | 4h       | Q1     |
| Q3   | 私聊交互与上下文维护        | P2     | 4h       | Q2     |
| Q4   | 用户绑定与通知推送          | P2     | 4h       | A8, A9 |
| Q5   | 防刷机制与内容过滤          | P3     | 2h       | C6     |

---

### Phase 3: 部署与测试

**负责人：全员协作**

| 序号 | 任务                 | 优先级 | 预估工时 | 依赖     |
| ---- | -------------------- | ------ | -------- | -------- |
| D1   | Docker容器化配置     | P1     | 4h       | 后端完成 |
| D2   | Nginx反向代理配置    | P1     | 2h       | D1       |
| D3   | 数据库备份与恢复脚本 | P2     | 2h       | A3       |
| D4   | 功能测试与Bug修复    | P1     | 8h       | 所有模块 |
| D5   | 性能测试与优化       | P2     | 4h       | D4       |

---

## 三、3人分工计划

### 第一周：核心功能冲刺（7.4-7.10）

**A同学（后端）：**
- 完成 A1-A5（项目初始化、数据库、物品CRUD、向量检索）
- 配合 B同学 联调API

**B同学（前端）：**
- 完成 B1-B4（项目初始化、首页、发布页、搜索页）
- 修复现有bug（表单验证、发布页面卡住）

**C同学（AI）：**
- 完成 C1-C2（Embedding封装、OCR集成）
- 配合 A同学 集成向量化逻辑

### 第二周：智能匹配与完善（7.11-7.17）

**A同学（后端）：**
- 完成 A6-A7（智能匹配引擎、挂失管理）
- 启动定时任务开发

**B同学（前端）：**
- 完成 B5-B6（个人中心、样式优化）
- 测试并修复联调问题

**C同学（AI）：**
- 完成 C3-C4（自然语言解析、定时任务）
- 开始QQ机器人集成

### 第三周：QQ集成与部署（7.18-7.24）

**A+C同学：**
- 完成 Q1-Q4（QQ机器人完善）

**B同学：**
- 完成 B7-B8（用户认证、地图定位）
- 整体UI优化

**全员：**
- 完成 D1-D5（部署与测试）

---

## 四、关键设计问题与建议

### 问题1：用户认证方式

**当前设计**：未明确登录方式

**建议方案**：
- 方案A：校园统一身份认证（对接学校OAuth）
- 方案B：手机号/邮箱注册 + 验证码
- 方案C：QQ扫码登录（与机器人绑定联动）

> **建议优先选方案C**：与QQ机器人联动，用户体验更流畅，无需额外注册。

### 问题2：OCR服务选择

**当前设计**：计划使用 Qwen-VL

**建议方案**：
- 如果学校提供API，优先使用学校API
- 否则考虑本地部署轻量级OCR模型（如 PaddleOCR）
- 备选：百度OCR、阿里云OCR（免费额度有限）

### 问题3：定位功能实现

**当前设计**：浏览器API获取GPS定位

**建议方案**：
- 基础版：浏览器GPS + 逆地理编码（腾讯/高德地图API）
- 简化版：提供常用地点下拉选择（图书馆、教学楼等）

> **建议先实现简化版**：避免依赖外部地图API，降低开发复杂度。

### 问题4：匹配阈值设定

**当前设计**：
- 匹配成功：相似度 ≥ 0.75
- 疑似匹配：0.5 ≤ 相似度 < 0.75
- 无匹配：相似度 < 0.5

**建议**：上线后根据实际数据调整阈值，初期可设为：
- 匹配成功：≥ 0.80（更严格，减少误匹配）
- 疑似匹配：0.60 ~ 0.80
- 自动通知阈值：≥ 0.85（避免频繁推送）

---

## 五、里程碑检查点

| 时间 | 里程碑        | 检查内容                 |
| ---- | ------------- | ------------------------ |
| 7.7  | 后端API第一版 | 物品CRUD、向量检索可用   |
| 7.10 | 前端核心功能  | 发布、搜索页面可用       |
| 7.14 | 智能匹配上线  | 自动匹配+通知可用        |
| 7.18 | QQ机器人完善  | 群聊+私聊功能可用        |
| 7.24 | 项目交付      | 全功能测试通过，部署上线 |

---

*更新时间：2026-07-05*

1. 完善数据库设计
- 确定使用什么embedding模型
- 设计物品表，比如物品状态、图片功能
- 设计用户表，比如用户状态、登录信息

2. 优化用户体验
- 比如传一张图片，自动识别物品信息
- 自动定位用户位置（最好可以解析成学校楼的名称）

3. AI agent相关
- 工具（Tools）定义：将search_items, create_lost, notify_match, get_banli封装为@tool
- 意图识别：解析群聊@消息，判断是“挂失/捡到/查询/闲聊”，分发到不同工具
- RAG检索增强：用户查询时，先从Pgvector拿Top5相似记录作为Prompt上下文，再让大模型生成回复
- 


